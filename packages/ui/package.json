{
  "name": "ui",
  "version": "1.0.0",
  "main": "./index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint components --fix",
    "lint:style": "stylelint \"**/*.scss\" --fix",
    "test": "vitest --run",
		"test:watch": "vitest --reporter verbose"
  }
}